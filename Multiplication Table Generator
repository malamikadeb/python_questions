Write a Python program that generates and displays the multiplication table for a given number using loops. The program should show the complete table from 1 to 10 in a formatted manner.



def print_multiplication_table(num):
    print(f"Multiplication Table for {num}: ")
    for i in range(1, 11):
        # for j in range(1,11):
        result = num * i
        print(f"{num} x {i} =  {result}")
    print()
num = 5

# Function call
print_multiplication_table(num)

Write a Python program that calculates the sum of factorials from 1! to n!. For example, if n = 4, compute 1! + 2! + 3! + 4!.



import math
def sum_of_factorials(n):
    # Code Starts
    total_factorial = 0
    for i in range(1, n + 1):
        total_factorial += math.factorial(i)
    return total_factorial
    # Code Ends

# Predefined inputs
num = 4

# Function call
print(sum_of_factorials(num))

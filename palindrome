Given a number num = 121, write a Python program using a while loop to check if it is a palindrome (same when reversed) and print True if it is, otherwise False.

def main():
    original_num = 121
    num = original_num
    reversed_num = 0
    while num > 0:
        digit = num % 10
        num = num// 10
        reversed_num = reversed_num * 10 + digit
    print(original_num == reversed_num)




if __name__ == "__main__":
    main()
    
